### Description
This repo houses sample scripts for agentic flows

### Setup
using pip on uv install the necessary python packages
this repo was tested with `python3.10`

if using `uv` as package manager
`uv pip install -r requirements.txt`

else if using `pip` as package manager
`pip install -r requirements.txt`
